{
  "name": "marketing-canvas-api",
  "version": "1.0.0",
  "private": true,
  "description": "Mock API for the Marketing Canvas frontend build. Zero dependencies.",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "reset": "rm -rf .data && node -e \"console.log('Data cleared. Next start reseeds.')\"",
    "test": "node --test test/*.test.mjs"
  }
}
